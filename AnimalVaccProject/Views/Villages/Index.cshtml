@model IEnumerable<AnimalVaccProject.Models.tbl_Village>

@{
    ViewBag.Title = "Index";
}

<h2>قائمة القرى</h2>

<p>
    @Html.ActionLink("إضافة جديد", "Create")
</p>
<div class="lstborder">
    <table class="table">
        <tr>
            <th>
                النيابة
            </th>
            <th>
                القرية
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.niyabat.NName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.VName)
        </td>
        <td>
            @Html.ActionLink("تعديل", "Edit", new { id=item.VID }) |
            @Html.ActionLink("تفاصيل", "Details", new { id=item.VID }) |
            @Html.ActionLink("حذف", "Delete", new { id=item.VID })
        </td>
    </tr>
}

    </table>
    </div>
